name: Documentation

on:
  push:
    branches: [main]
    tags:
      - "[0-9]+.[0-9]+.[0-9]+"

concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: true

jobs:
  docs:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v6
      - name: Configure Git Credentials
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
      - name: Install uv and set up Python
        uses: astral-sh/setup-uv@v7
        with:
          python-version: 3.14
          activate-environment: true
      - name: Install the project
        run: uv sync --group docs
      - name: Authenticate (J-Quants)
        env:
          J_QUANTS_MAILADDRESS: ${{ secrets.J_QUANTS_MAILADDRESS }}
          J_QUANTS_PASSWORD: ${{ secrets.J_QUANTS_PASSWORD }}
        run: uv run kabu auth jquants --mailaddress "$J_QUANTS_MAILADDRESS" --password "$J_QUANTS_PASSWORD"
      - name: Authenticate (EDINET)
        env:
          EDINET_API_KEY: ${{ secrets.EDINET_API_KEY }}
        run: uv run kabu auth edinet --api-key "$EDINET_API_KEY"
      - name: Cache kabukit data
        id: cache-data
        uses: actions/cache@v4
        with:
          path: ~/.cache/kabukit
          key: docs-data-${{ runner.os }}-${{ hashFiles('**/uv.lock') }}
      - name: Debug - Check cache after restore
        run: |
          echo "=== Cache hit: ${{ steps.cache-data.outputs.cache-hit }}"
          echo "=== Contents of ~/.cache/kabukit:"
          ls -laR ~/.cache/kabukit || echo "Directory does not exist"
          echo "=== Parquet files:"
          find ~/.cache/kabukit -name "*.parquet" -ls || echo "No parquet files found"
      - name: Get data if cache not found
        if: steps.cache-data.outputs.cache-hit != 'true'
        run: |
          uv run kabu get jquants --all -q
          uv run kabu get edinet --all -q
      - name: Debug - Check cache after get
        run: |
          echo "=== Contents of ~/.cache/kabukit after get:"
          ls -laR ~/.cache/kabukit || echo "Directory does not exist"
          echo "=== Parquet files:"
          find ~/.cache/kabukit -name "*.parquet" -ls || echo "No parquet files found"
      - name: Deploy documentation
        run: uv run mkdocs gh-deploy --force
